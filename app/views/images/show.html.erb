<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>"><%= value %></div>
<% end %>

<div class="row">
  <div class="col-6">
    <%= image_tag(@image.url) %>
  </div>
  <div class="col-6">
    <b>Image Tags:</b>
    <span><%= @image.tag_list %></span>
  </div>
</div>
